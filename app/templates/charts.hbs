<h1>These are your charts</h1>

<ul>
  {{#each model as |chart|}}
    <li>{{chart.title}}: {{chart.progress}}%</li>
  {{/each}}
</ul>

{{create-new-chart onCreateNewChart=(action "createNewChart")}}

{{outlet}}